<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.code">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".exoplayer.ExoActivity" />

        <activity android:name=".html.HTMLActivity" />

        <activity android:name=".view.expandableTextView.ExpandableTextActivity" />

        <activity android:name=".kbpanel.test.KeyboardActivity" />


        <!-- keep live -->
        <activity android:name=".keeplive.KeepLiveDemoActivity" />

        <!-- keep live one px -->
        <activity
            android:name=".keeplive.onepx.KeepLiveActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:finishOnTaskLaunch="false"
            android:taskAffinity="com.sososeen09.daemon.keep.live"
            android:theme="@style/KeepLiveTheme" />
        <service
            android:name=".keeplive.onepx.KeepLiveService"
            android:process=":live_service" />


        <!-- keep live foreground notification -->
        <service android:name=".keeplive.notification.KeepLiveService" />
        <service android:name=".keeplive.notification.KeepLiveService$InnerService" />

        <!-- keep live job -->
        <service android:name=".keeplive.job.KeepLiveJob" />


        <!-- keep live -->
        <activity android:name=".NetActivity" />

    </application>

</manifest>